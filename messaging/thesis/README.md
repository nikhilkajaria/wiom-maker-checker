# Thesis — L1 / L2 / L3

**Owner:** Shiva (`@shivakimothi-design`)

Audience-layer definitions, objectives, differentiators, bridges, and must-not-do rules.

- `audience_layers.json` — ✅ moved here from `reference/` (history preserved). The layer
  definitions that the gate logic's L1/L2/L3 strictness dial refers to.

**Read-dependency:** the *need-state vocabulary* lives in `/wrapper/test-variables/` (Nikhil),
but its **per-layer expression** is thesis (here). Keep the vocabulary list in wrapper; keep
the per-layer expressions here. See [[need-states-read-dependency]].
