# Expressions

**Owner:** Shiva (`@shivakimothi-design`) · **Status:** 🟡 to extract

The contrast doctrine, permitted/forbidden expressions, and destination tests.

**To do:** extract from *Contrast Doctrine v2.1* (source doc) into clean files here. Until
extracted, the operative version is embedded in `reference/context_layer.md` §4 (capture/
convert split) and the gate logic.
