# G1 / G2 Logic

**Owner:** Shiva (`@shivakimothi-design`) · **Status:** ✅ ratified v1.1

- [`v1.md`](v1.md) — the ratified, **layer-aware** G1 + G2 rules (the source of truth).
- `dangerous_misreads.json` — ✅ moved here from `reference/` (history preserved). The
  misread weighting the gates reference (mobile-data product = primary).

Versioning: add `v2.md` etc. for future versions; keep old versions for audit. Any change
here is a PR that Shiva approves.
