# Formats Spec

**Owner:** Nikhil (`@nikhilkajaria`) · **Status:** 🔴 missing — needs creating

Aspect ratios, durations, safe zones, caption rules — the production format contract.

**Why it matters:** this **blocks the post-prod hygiene check (P3)** in
`/evaluation/agents/`. Until it exists, the post-prod agent's format-hygiene lens stays a
documented placeholder.

**To do (Nikhil):** create the formats-spec (currently scattered across production specs /
the Creative Input sheet).
