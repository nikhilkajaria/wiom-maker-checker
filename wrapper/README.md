# Wrapper Framework

**Owner:** Nikhil (`@nikhilkajaria`) · Approval required via CODEOWNERS
**Status legend:** ✅ in place · 🟡 scattered / needs extraction · 🔴 missing

How we build it: the four test variables, the determined parameters, and the formats-spec.

| Sub-folder | What lives here | Status |
|---|---|---|
| `test-variables/` | The 4 test variables + current values; need-state vocabulary | 🟡 Nikhil confirms current version |
| `determined-params/` | CTA by stage, language by geo, register | 🟡 extract from Engine Explainer |
| `formats-spec/` | Ratios, durations, safe zones, caption rules | 🔴 **Nikhil to create — blocks post-prod P3 hygiene** |

**Source doc (moved here from `docs/source_docs/`):** `Wiom_Performance_Creative_Framework_v0_9.docx`
— the original the test-variables / determined-params are confirmed against.

**Need-state note:** need-state is a wrapper test-variable (here), but its expression *per
layer* is thesis (Shiva). Keep the vocabulary list here; the per-layer expressions live in
`/messaging/thesis/`. See [[need-states-read-dependency]].
